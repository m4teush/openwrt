${devtype} read ${loadaddr} 0 1
ptuuid ${loadaddr} ptuuid
setenv bootargs "${console} ${mtdparts} root=PARTUUID=${ptuuid}-02 rw rootwait video=DVI-D-1:1280x1024"

${fs}load ${devtype} ${devnum}:${distro_bootpart} ${kernel_addr_r} uImage
bootm ${kernel_addr_r}
